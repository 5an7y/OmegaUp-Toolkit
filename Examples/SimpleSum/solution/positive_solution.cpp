#include <bits/stdc++.h>
using namespace std;

uint64_t a, b;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> a >> b;
    cout << a + b << '\n';

    return 0;
}