#include <bits/stdc++.h>
using namespace std;

int64_t a, b, n;
bool isneg = false;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> n >> a >> b;
    cout << a + b;
    return 0;
}